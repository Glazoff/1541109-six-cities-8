/* eslint-disable camelcase */

export type userType = {
  avatarUrl: string,
  email: string,
  id: number,
  isPro: boolean,
  name: string,
  token: string,
}

export type userAdapter = {
  avatar_url: string,
  email: string,
  id: number,
  is_pro: boolean,
  name: string,
  token: string,
}
